  - The code actually compiles with CLANG. Try it on OS X and adjust building instructions.
  - Add GPU kernel to the Python and MATLAB wrappers.
  - Windows and OS X support for SSE and CUDA kernels.
  - Add padding to support matrices of arbitrary size.
  - Upgrade SSE kernel to AVX.
  - Update CUDA kernel from Compute Capability 2.0 to 5.0.
